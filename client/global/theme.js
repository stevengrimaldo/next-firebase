const baseColors = {
  black: '#000000', // black
  transparent: 'transparent', // transparent
  white: '#ffffff', // white
}

const primaryColors = {}

const secondaryColors = {}

const controlColors = {}

const auxiliaryColors = {}

export const color = {
  ...baseColors,
  ...primaryColors,
  ...secondaryColors,
  ...controlColors,
  ...auxiliaryColors,
}

export default {
  color,
}
